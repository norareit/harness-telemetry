{
  "//": "Copy to ~/.config/harness-usage/config.json and edit. Never commit the real file.",

  "device": "desktop",

  "postgres": {
    "//": "rpi5 over Tailscale. Password is the POSTGRES_PASSWORD from server/.env.",
    "dsn": "postgres://harness:CHANGE_ME@100.x.y.z:5432/harness",
    "connectionTimeoutMillis": 10000,
    "statementTimeoutMillis": 30000
  },

  "sources": {
    "claude-code": {
      "enabled": true,
      "root": "~/.claude/projects",
      "//billing": "'free' = subscription (Max), 'api' = paid per token. Sets the `billing` column on priced rows.",
      "billing": "free"
    },
    "opencode": {
      "enabled": true,
      "db": "~/.local/share/opencode/opencode.db",
      "billing": "free"
    }
  },

  "pricing": {
    "modelsJson": "~/.cache/opencode/models.json",
    "//overrides": "null = use the bundled agent/pricing-overrides.json",
    "overrides": null
  },

  "//scenarios": [
    "Counterfactual targets materialized into Postgres for the dashboard.",
    "Keys are fully-qualified '<provider>/<model>' as they appear in models.json —",
    "note GLM is not on OpenRouter, it comes via gateways like tokengo.",
    "`harness-usage compare --as <key>` works against ANY model in the table,",
    "so this list only needs the ones you want on the dashboard.",
    "Set to [] to skip scenario computation entirely."
  ],
  "scenarios": [
    "openrouter/anthropic/claude-opus-5",
    "openrouter/anthropic/claude-sonnet-5",
    "openrouter/openai/gpt-6-astra",
    "openrouter/google/gemini-3.1-pro-preview",
    "openrouter/qwen/qwen3.7-flash",
    "openrouter/moonshotai/kimi-k2.7-code",
    "tokengo/z-ai/glm-5.2"
  ]
}
